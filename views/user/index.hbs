
        <form class="col-md-4" action="/search" method="post">
   <input class="form-control" type="search" name="keyword" id="search" required placeholder="Search by toy name">
</form>
   <ul>
<body>
   <table class="table table-primary">
  <tr>
    <th>Toy name</th>
    <th>Toy price</th>
    <th>collection</th>
    <th>Toy image</th>
  </tr>
  {{#each toys}}
    <tr>
      <td>{{name}}</td>
      <td>{{price}}</td>
      <td>{{collection.name}}</td>
      <td>
        <img src="{{image}}" alt="" width="100" height="120" />
      </td>
    </tr>
  {{/each}}
</table>
</body>
   </ul>
</nav>